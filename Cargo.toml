[package]
name = "megastore_search"
version = "0.1.0"
edition = "2024"

[dependencies]
petgraph = "0.6"   # Para grafos
serde = { version = "1.0", features = ["derive"] } # Para serialização
serde_json = "1.0" # Para exportar/importar dados
