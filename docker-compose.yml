version: '3.8'
services:
  mysql:
    image: mysql:8.0
    container_name: megastore_mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: senha123
      MYSQL_DATABASE: megastore
    ports:
      - "3306:3306"
    volumes:
      - ./mysql_data:/var/lib/mysql
